__pycache__/
*.pyc
build/
dist/
*.spec
*.clean.*
# Large generated artifact (delivered as a release/download, not via git).
# Reproduce with build_exe_wine.sh (Linux+Wine) or build_standalone_msi.bat (Windows).
AISoundStripper-Standalone.msi
