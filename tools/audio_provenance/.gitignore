__pycache__/
*.pyc
build/
dist/
*.spec
*.clean.*
# Large generated installer (delivered as a release/download, not via git).
# Reproduce with build_exe_wine.sh (Linux+Wine) or build_exe.bat + makensis (Windows).
msi/AISoundStripper-Setup.exe
